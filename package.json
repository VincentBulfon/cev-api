{
  "dependencies": {
    "@prisma/client": "2.23",
    "apollo-server": "^3.1.2",
    "graphql": "^15.5.1",
    "nexus": "^1.1.0",
    "nexus-plugin-prisma": "^0.35.0"
  },
  "devDependencies": {
    "@types/node": "^16.6.1",
    "prisma": "^2.29.0",
    "ts-node": "^10.2.0",
    "ts-node-dev": "^1.1.8",
    "typescript": "^4.3.5"
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "ts-node-dev --transpile-only --no-notify api/index.ts",
    "cleanup": "node node_scripts/cleanup.js",
    "build": "tsc"
  }
}
